# shellcheck shell=bash

env_vars_required GITHUB_TOKEN

use flake .#terraform

TF_CLI_CONFIG_FILE="$PWD/config.tfrc"
TF_VAR_passphrase="$(sops -d --extract '["passphrase"]' secrets.yaml)"
export TF_CLI_CONFIG_FILE TF_VAR_passphrase

moved {
  from = hydra_jobset.nixpkgs_jobset["bsd"]
  to   = hydra_jobset.bsd
}

moved {
  from = hydra_jobset.nixpkgs_jobset["bsd_staging_next"]
  to   = hydra_jobset.bsd_staging_next
}

moved {
  from = hydra_jobset.nixpkgs_jobset["cuda"]
  to   = hydra_jobset.cuda
}

moved {
  from = hydra_jobset.nixpkgs_jobset["cuda_stable"]
  to   = hydra_jobset.cuda_stable
}

moved {
  from = hydra_jobset.nixpkgs_jobset["cuda_stable_previous"]
  to   = hydra_jobset.cuda_stable_previous
}

moved {
  from = hydra_jobset.nixpkgs_jobset["rocm"]
  to   = hydra_jobset.rocm
}

moved {
  from = hydra_jobset.nixpkgs_jobset["unfree_redist"]
  to   = hydra_jobset.unfree_redist
}

moved {
  from = hydra_jobset.nixpkgs_jobset["unfree_redist_darwin"]
  to   = hydra_jobset.unfree_redist_darwin
}

moved {
  from = hydra_jobset.nixpkgs_jobset["unfree_redist_full"]
  to   = hydra_jobset.unfree_redist_full
}

moved {
  from = hydra_jobset.nixpkgs_jobset["unfree_redist_stable"]
  to   = hydra_jobset.unfree_redist_stable
}

moved {
  from = hydra_jobset.nixpkgs_jobset["unfree_redist_stable_previous"]
  to   = hydra_jobset.unfree_redist_stable_previous
}
